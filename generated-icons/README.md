# Generated Icon Source Assets

These PNG files are design-source and preview assets used while creating the FileSyncMonitor icon direction.

The application does not load files from this folder at runtime. Runtime bundle icons live in `sync-kernel/icons/` and are referenced by `sync-kernel/tauri.conf.json`.

Keep this folder in git so future icon refreshes can trace the source artwork and preview compositions.

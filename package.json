{
  "name": "tauri-scaffold",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "cd sync-kernel && tauri dev",
    "build": "cd sync-kernel && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
